# ADR-0002：持久化异步索引任务

- 状态：已采用
- 日期：2026-08-04

## 背景

Embedding、Reranker 或 ChromaDB 不可用时，同步索引会让用户无法保存知识。

## 决策

页面事务只创建 `WikiIndexTask`，进程内 Worker 分批消费。任务记录状态、尝试次数、下次执行时间和锁定时间，失败采用指数退避，启动时恢复中断任务。

## 结果

- 主数据写入不依赖模型可用性。
- 单机部署不需要额外消息中间件。
- 当前 Worker 不支持跨节点竞争；扩展到多实例时应迁移到具备租约或确认语义的独立队列。
